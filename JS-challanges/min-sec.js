/**
 * Write a function to convert minutes into seconds. Return the total number of seconds for the given number of minutes.
 * input: 5     Output: 300
 */
function minToSec(minutes){
    let seconds = minutes * 60;
    return seconds;
}
let sec = minToSec(6);
console.log(sec);











