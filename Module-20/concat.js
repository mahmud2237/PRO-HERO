const first = 'Abid';
const last = 'Naim';
//console.log(first, last);
//console.log(first + ' ' + last);
// Let's use concat to generate a full name
const fullName = first.concat(' ').concat(last);
console.log(fullName);