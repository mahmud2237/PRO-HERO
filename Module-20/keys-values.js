const computer = {
    brand: 'lenovo',
    price: 54000,
    processor: 'intel',
    sdd: '512gb',
}

// To Get all keys and values of the object
const keys = Object.keys(computer);
console.log('Keys are: ', keys);


const values = Object.values(computer);
console.log('Values are:', values);